#include "catch.h"
extern "C" {
    #include "uv.h"
    #include "rewind.h"
}

SCENARIO("rewind is initialized", "[rewind]") {
    GIVEN("A new database") {
        WHEN("created") {
            THEN("set key") {
            }
        }
    }
}